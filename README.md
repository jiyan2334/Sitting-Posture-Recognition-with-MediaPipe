# 坐姿检测项目（Sitting Posture Recognition）

## 项目简介

本项目使用 MediaPipe 实现实时坐姿检测，能够识别不良坐姿并给出相应提醒。

## 项目架构

### 目录结构

```
Sitting-Posture-Recognition-with-MediaPipe/
├── app.py                 # 新的主入口文件（整合所有功能）
├── main.py                # 原有主入口文件（保持不变）
├── src/
│   ├── config/            # 配置模块
│   │   ├── __init__.py
│   │   └── settings.py    # 应用配置类
│   ├── core/              # 核心功能模块
│   │   ├── __init__.py
│   │   ├── tracking/      # 时间统计和日志生成
│   │   │   ├── __init__.py
│   │   │   └── tracking.py # 坐姿持续时间监测和日志生成
│   │   ├── reminder/      # 提醒功能
│   │   │   ├── __init__.py
│   │   │   └── reminder.py # 多模态提醒功能
│   │   └── ui/            # UI功能
│   │       ├── __init__.py
│   │       └── ui_handler.py # 优化的UI设计和响应式布局
│   ├── detector/          # 检测模块
│   │   ├── __init__.py
│   │   └── pose_detector.py # 姿态检测核心逻辑
│   └── utils/             # 工具模块
│       ├── __init__.py
│       └── pose_utils.py  # 姿态检测相关工具函数
├── .gitignore
└── README.md
```

### 模块职责

1. **配置模块（config）**
   - `settings.py`：存储应用配置，如坐姿阈值、提醒方式、窗口大小等。

2. **核心功能模块（core）**
   - **时间统计和日志生成（tracking）**：
     - `tracking.py`：监测坐姿持续时间，记录每种坐姿的开始时间和持续时间，支持会话数据的保存和日志生成。
   - **提醒功能（reminder）**：
     - `reminder.py`：实现多模态提醒，包括声音提醒和系统通知。
   - **UI功能（ui）**：
     - `ui_handler.py`：实现优化的UI设计，包括坐姿状态显示、持续时间显示、操作提示等，支持响应式布局和全屏模式。

3. **检测模块（detector）**
   - `pose_detector.py`：使用 MediaPipe 进行人体关键点检测，判断坐姿状态。

4. **工具模块（utils）**
   - `pose_utils.py`：提供姿态检测相关的工具函数，如角度计算等。

5. **主入口文件**
   - `main.py`：原有主入口文件，保持原有功能。
   - `app.py`：新的主入口文件，整合所有模块，实现完整的坐姿检测和提醒功能。

### 数据流

1. **输入**：摄像头画面 → `app.py`
2. **检测**：`app.py` → `pose_detector.py`（处理画面，返回坐姿状态）
3. **工具**：`pose_detector.py` → `pose_utils.py`（使用 `findAngle` 计算角度）
4. **业务逻辑**：
   - `app.py` → `tracking.py`（更新坐姿持续时间）
   - `app.py` → `reminder.py`（当持续时间超过阈值时触发提醒）
   - `app.py` → `ui_handler.py`（绘制UI，显示状态和提醒）
5. **输出**：处理后的画面 → 显示给用户

## 功能特性

1. **坐姿持续时间监测**：
   - 记录每种坐姿的持续时间。
   - 当不良坐姿持续超过设定阈值时触发提醒。
   - 支持会话数据的保存，便于后续分析。

2. **多模态提醒**：
   - 声音提醒：播放系统提示音。
   - 系统通知：发送桌面通知（可选）。
   - 视觉提醒：在界面上显示警告信息。

3. **优化的UI设计**：
   - 清晰的坐姿状态显示。
   - 实时显示坐姿持续时间。
   - 操作提示信息。
   - 响应式布局，支持窗口大小调整和全屏模式切换。

4. **响应式布局**：
   - 支持窗口大小调整。
   - 支持全屏模式切换。

## 安装依赖

## 使用方法

### 启动应用

1. **使用新功能**（整合所有拓展功能）：
   ```bash
   python app.py
   ```

2. **使用原有功能**：
   ```bash
   python main.py
   ```

### 操作说明

- 按下 `q` 键：退出应用。
- 按下 `f` 键：切换全屏模式。

### 配置调整

打开 `src/config/settings.py` 文件，修改配置参数：

- `POSTURE_THRESHOLD`：不良坐姿持续时间阈值（秒）。
- `ENABLE_SOUND`：是否启用声音提醒。
- `ENABLE_NOTIFICATION`：是否启用系统通知。
- `WINDOW_WIDTH`、`WINDOW_HEIGHT`：窗口大小。
- `ENABLE_FULLSCREEN`：是否默认启用全屏模式。

## 架构设计原则

1. **高内聚**：
   - 每个模块都有明确的职责，专注于自己的功能。

2. **低耦合**：
   - 模块间通过明确的接口通信，没有直接依赖。
   - 便于后续的功能扩展和维护。

3. **可复用性**：
   - `utils` 中的工具函数可以在其他项目中直接使用。
   - `detector` 中的检测逻辑可以被其他需要姿态检测的应用复用。

4. **可测试性**：
   - 各模块可以独立测试，便于发现和修复问题。

## 扩展建议

1. **添加新功能**：
   - 在 `core` 目录下创建新的子模块，实现更多功能。

2. **优化检测算法**：
   - 修改 `detector` 目录下的代码，使用更先进的检测算法。

3. **添加数据可视化**：
   - 基于 `tracking` 模块保存的数据，添加数据可视化功能。

4. **跨平台支持**：
   - 确保在 Windows、macOS 和 Linux 等主流操作系统上都能正常运行。

## 贡献指南

1. **提交规范**：
   - 使用清晰的提交信息，描述代码变更。
   - 提交前确保代码通过测试。

2. **分支管理**：
   - 使用形如 xxx-feature 的分支开发新功能。

## 许可证

本项目采用 MIT 许可证，详见 LICENSE 文件。
